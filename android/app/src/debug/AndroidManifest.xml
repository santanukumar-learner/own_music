<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Required so the Flutter tooling/observatory can connect over the network
         while debugging. -->
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
