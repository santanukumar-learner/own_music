# Sonata enrichment service — Python dependencies.
# Pinned for reproducibility; adjust if your platform needs different wheels.
# Tested target: Python 3.11 / 3.12.

fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20        # FastAPI file uploads
python-dotenv==1.0.1

pyacoustid==1.3.0               # AcoustID + Chromaprint (needs libchromaprint, see README)
musicbrainzngs==0.7.1          # MusicBrainz client
requests==2.32.3               # Last.fm / Discogs / Cover Art Archive

numpy==1.26.4
scipy==1.13.1
soundfile==0.12.1              # in-memory decode (libsndfile 1.1+ handles mp3/flac/ogg/wav)
librosa==0.10.2.post1         # BPM, key, chroma, spectral features
onnxruntime==1.20.1           # genre/mood model inference
